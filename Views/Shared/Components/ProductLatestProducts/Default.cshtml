@model IEnumerable<WebUITopic5_Team4.Models.ViewModels.ProductCardViewModel>
@{
    var latestProducts = Model.ToList();
    var slides = latestProducts.Chunk(3).ToList();
}


        <div class="latest-product__slider owl-carousel">
            @foreach (var slide in slides)
            {
                <div class="latest-product__slider__item">
                    @foreach (var item in slide)
                    {
                        <div class="latest-product__item">
                            <div class="latest-product__item__pic">
                                <img src="@Url.Content(item.PrimaryImage ?? "~/images/Others/lp-1.jpg")" alt="@item.ProductName" />
                            </div>
                            <div class="latest-product__item__text">
                                <h6>@item.ProductName</h6>
                                <span>@item.FinalPrice.ToString("N0")₫</span>
                            </div>
                        </div>
                    }
                </div>
            }
        </div>

